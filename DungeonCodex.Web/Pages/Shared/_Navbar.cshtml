@using DungeonCodex.Web.Lib.Dependencies;
@inject IBasePageDependencies dependencies
@{
    var currentUser = dependencies.UserManager.GetUserAsync(Context.User).Result;
    bool isAdmin() => currentUser?.IsAdmin ?? false;
}
<nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
    <div class="container">
        <a class="navbar-brand" asp-area="" asp-page="/Index">DungeonCodex</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
            <ul class="navbar-nav flex-grow-1">
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-page="/Index">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-page="/Privacy">Privacy</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-page="/Campaigns/Index">Campaigns</a>
                </li>
                @if (isAdmin())
                {
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-area="" asp-page="/Admin/Index">Admin Dashboard</a>
                    </li>
                })
                <li class="nav-item">

                </li>
            </ul>
        </div>

        <!--Sign in area-->
        <div style="text-align:right;">
            @if (User?.Identity?.IsAuthenticated ?? false)
            {
                @*<p>
            @foreach (var claim in Context.User.Claims)
            {
            <div><code>@claim.Type</code>: <strong>@claim.Value</strong></div>
            }
            </p>
            *@
                <p style="color:black">
                    Signed in as <strong>@User.Identity.Name</strong>
                </p>
                <a class="btn btn-primary btn-sm" id="signoutbtn" asp-area="" asp-controller="Account" asp-action="SignOutCurrentUser">Sign Out</a>
            }
            else
            {
                <a class="btn btn-primary btn-sm" asp-route-returnUrl="@Context.Request.Path.Value" asp-area="" asp-controller="Account" asp-action="DiscordLogin">Sign In <i class="fa-brands fa-discord"></i></a>
            }
        </div>
    </div>
</nav>